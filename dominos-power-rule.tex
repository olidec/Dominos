\documentclass{article}
\usepackage{amsmath,amssymb}
\RequirePackage{tikz}
\usetikzlibrary{plotmarks, math}
\usepackage{pgfplots}
\pgfplotsset{compat=1.16}
\usepackage[margin=0cm, landscape]{geometry}

\setlength{\parindent}{0cm}
\setlength{\lineskip}{10pt}

    \newcommand{\hdomi}[2]{
        \begin{tikzpicture}
            \tikzmath{
                \dwidth = 13.5;
                \dheight = 4.5;
            }
        \draw[thick] (0,0) rectangle (\dwidth,\dheight);
        \draw (\dwidth/2,0) -- (\dwidth/2,\dheight);
        \node at (\dwidth/4,\dheight/2) {#1};
        \node at (3*\dwidth/4,\dheight/2) {#2};
        \end{tikzpicture}
    } 

    \newcommand{\vdomi}[2]{
        \begin{tikzpicture}
            \tikzmath{
                \dwidth = 8;
                \dheight = 3.5;
            }
        \draw[thick] (0,0) rectangle (\dwidth,\dheight);
        \draw (0,\dheight/2) -- (\dwidth,\dheight/2);
        \node at (\dwidth/2,3*\dheight/4) {\Large #1};
        \node at (\dwidth/2,\dheight/4) {\Large #2};
        \end{tikzpicture}
    }

    \newcommand{\myplot}[5]{
     \begin{tikzpicture}
          \begin{axis}[
               grid = both,
               axis lines = middle,
               xlabel = $x$,
               x label style={anchor=west},
               ylabel = $y$,
               y label style={anchor=south},
               xmin = #2 , xmax = #3,
               ymin = #4, ymax = #5,
               restrict y to domain=#4:#5,
               xtick distance=1,
               ytick distance=1,
              ]
              \addplot[line width=1mm, domain=#2*1.1:#3*1.1, samples=100, color=red]{#1};
          \end{axis}
      \end{tikzpicture}
}

\newcommand{\myvplot}[5]{
     \begin{tikzpicture}
          \begin{axis}[
               grid = both,
               axis lines = middle,
               xlabel = $x$,
               x label style={anchor=west},
               ylabel = $y$,
               y label style={anchor=south},
               xmin = #2 , xmax = #3,
               ymin = #4, ymax = #5,
               restrict y to domain=#4:#5,
               xtick distance=1,
               ytick distance=1,
              ]
              \addplot [line width=1mm, samples=100, color=red]coordinates {(#1,#5) (#1,#4)};
          \end{axis}
      \end{tikzpicture}
}

\newcommand{\plotbox}[5]{
    \resizebox{0.2\textwidth}{!}{\myplot{#1}{#2}{#3}{#4}{#5}}
}

\newcommand{\vplotbox}[5]{
    \resizebox{0.2\textwidth}{!}{\myvplot{#1}{#2}{#3}{#4}{#5}}
}
\pagestyle{empty}
\begin{document}
\begin{center}\hdomi{START}{$f(x) = \dfrac{5}{x^2}$.}
\hdomi{$f'(x) = -\dfrac{10}{x^3}$}{$f(x) = \sqrt[4]{x^3}$.}
\hdomi{$f'(x) = \dfrac{3}{4\sqrt[4]{x}}$}{$f(x) = x^8$.}
\hdomi{$f'(x) = 8x^7$}{$f(x) = 5x^3$.}
\hdomi{$f'(x) = 15x^2$}{$f(x) = \sqrt[3]{x}$.}
\hdomi{$f'(x) = \dfrac{1}{3\sqrt[3]{x^2}}$}{$f(x) = x^{3/2}$.}
\hdomi{$f'(x) = \dfrac{3}{2}\sqrt{x}$}{$f(x) = x^{2/3}$.}
\hdomi{$f'(x) = \dfrac{2}{3\sqrt[3]{x}}$}{$f(x) = \dfrac{2}{\sqrt{x}}$.}
\hdomi{$f'(x) = -\dfrac{1}{\sqrt{x^3}}$}{$f(x) = \sqrt{x}$.}
\hdomi{$f'(x) = \dfrac{1}{2\sqrt{x}}$}{$f(x) = 4x^{-3}$.}
\hdomi{$f'(x) = -12x^{-4}$}{$f(x) = \dfrac{1}{x}$.}
\hdomi{$f'(x) = -\dfrac{1}{x^2}$}{$f(x) = 4\sqrt{x}$.}
\hdomi{$f'(x) = \dfrac{2}{\sqrt{x}}$}{$f(x) = x^{-2}$.}
\hdomi{$f'(x) = -2x^{-3}$}{$f(x) = -\dfrac{3}{x^4}$.}
\hdomi{$f'(x) = \dfrac{12}{x^5}$}{$f(x) = \dfrac{1}{x^3}$.}
\hdomi{$f'(x) = -\dfrac{3}{x^4}$}{END}

\end{center}
\end{document}