\documentclass{article}
\usepackage{amsmath,amssymb}
\usepackage{xcolor}
\RequirePackage{tikz}
\usetikzlibrary{plotmarks, math}
\usepackage{pgfplots}
\pgfplotsset{compat=1.16}
\usepackage[margin=0cm, landscape]{geometry}

\setlength{\parindent}{0cm}
\setlength{\lineskip}{10pt}

    \newcommand{\hdomi}[2]{
        \begin{tikzpicture}
            \tikzmath{
                \dwidth = 13.5;
                \dheight = 4.5;
            }
        \draw[thick] (0,0) rectangle (\dwidth,\dheight);
        \draw (\dwidth/2,0) -- (\dwidth/2,\dheight);
        \node at (\dwidth/4,\dheight/2) {#1};
        \node at (3*\dwidth/4,\dheight/2) {#2};
        \end{tikzpicture}
    } 

    \newcommand{\vdomi}[2]{
        \begin{tikzpicture}
            \tikzmath{
                \dwidth = 8;
                \dheight = 3.5;
            }
        \draw[thick] (0,0) rectangle (\dwidth,\dheight);
        \draw (0,\dheight/2) -- (\dwidth,\dheight/2);
        \node at (\dwidth/2,3*\dheight/4) {\Large #1};
        \node at (\dwidth/2,\dheight/4) {\Large #2};
        \end{tikzpicture}
    }

    \newcommand{\myplot}[5]{
     \begin{tikzpicture}
          \begin{axis}[
               grid = both,
               axis lines = middle,
               xlabel = $x$,
               x label style={anchor=west},
               ylabel = $y$,
               y label style={anchor=south},
               xmin = #2 , xmax = #3,
               ymin = #4, ymax = #5,
               restrict y to domain=#4:#5,
               xtick distance=1,
               ytick distance=1,
              ]
              \addplot[line width=1mm, domain=#2*1.1:#3*1.1, samples=100, color=red]{#1};
          \end{axis}
      \end{tikzpicture}
}

\newcommand{\myvplot}[5]{
     \begin{tikzpicture}
          \begin{axis}[
               grid = both,
               axis lines = middle,
               xlabel = $x$,
               x label style={anchor=west},
               ylabel = $y$,
               y label style={anchor=south},
               xmin = #2 , xmax = #3,
               ymin = #4, ymax = #5,
               restrict y to domain=#4:#5,
               xtick distance=1,
               ytick distance=1,
              ]
              \addplot [line width=1mm, samples=100, color=red]coordinates {(#1,#5) (#1,#4)};
          \end{axis}
      \end{tikzpicture}
}

\newcommand{\plotbox}[5]{
    \resizebox{0.2\textwidth}{!}{\myplot{#1}{#2}{#3}{#4}{#5}}
}

\newcommand{\vplotbox}[5]{
    \resizebox{0.2\textwidth}{!}{\myvplot{#1}{#2}{#3}{#4}{#5}}
}
\pagestyle{empty}
\begin{document}
\pagecolor{green!10}
\begin{center}\hdomi{START}{$f(x) = x^3 \cdot e^x$.}
\hdomi{$f'(x) = 3x^2 \cdot e^x + x^3 \cdot e^x$}{$f(x) = \dfrac{x^2}{x+1}$.}
\hdomi{$f'(x) = \dfrac{2x \cdot (x+1) - x^2 \cdot 1}{(x+1)^2}$}{$f(x) = 5x^3 - 2x + 1$.}
\hdomi{$f'(x) = 15x^2 - 2$}{$f(x) = \dfrac{\sin(x)}{x}$.}
\hdomi{$f'(x) = \dfrac{\cos(x) \cdot x - \sin(x) \cdot 1}{x^2}$}{$f(x) = (2x+3)^5$.}
\hdomi{$f'(x) = 5\cdot (2x+3)^4 \cdot 2$}{$f(x) = -2x^4 + 3x^2 - x$.}
\hdomi{$f'(x) = -8x^3 + 6x - 1$}{$f(x) = x^5 - 4x^3 + 2x^2$.}
\hdomi{$f'(x) = 5x^4 - 12x^2 + 4x$}{$f(x) = x^2 \cdot e^{2x}$.}
\hdomi{$f'(x) = 2x \cdot e^{2x} + x^2 \cdot e^{2x} \cdot 2$}{$f(x) = x \cdot \ln(x)$.}
\hdomi{$f'(x) = 1 \cdot \ln(x) + x \cdot \frac{1}{x}$}{$f(x) = x^2 \cdot \sin(x)$.}
\hdomi{$f'(x) = 2x \cdot \sin(x) + x^2 \cdot \cos(x)$}{$f(x) = \dfrac{e^x}{x^2}$.}
\hdomi{$f'(x) = \dfrac{e^x \cdot x^2 - e^x \cdot 2x}{(x^2)^2}$}{$f(x) = e^{3x+1}$.}
\hdomi{$f'(x) = e^{3x+1} \cdot 3$}{$f(x) = \dfrac{x}{x^2+1}$.}
\hdomi{$f'(x) = \dfrac{1 \cdot (x^2+1) - x \cdot 2x}{(x^2+1)^2}$}{$f(x) = \ln(x^2+1)$.}
\hdomi{$f'(x) = \dfrac{1}{x^2+1} \cdot 2x$}{$f(x) = \sin(x^2)$.}
\hdomi{$f'(x) = \cos(x^2) \cdot 2x$}{END}

\end{center}
\end{document}